package com.sss.composetablayout

import androidx.compose.ui.graphics.Color

/**
 * Default colors used in the tab layout
 */
object TabLayoutColors {
    val TabBackgroundColor = Color(0xFFE6E6E6) // Light gray for tab background
    val SelectedTabIndicatorColor = Color.White // White for selected tab indicator
    val TabTextColor = Color(0xFF666666) // Gray for tab text
    val SelectedTabTextColor = Color(0xFF000000) // Black for selected tab text
} 